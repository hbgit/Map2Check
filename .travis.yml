dist: xenial
sudo: require

language: cpp


before_install:
  - sudo apt-get install -y libboost-all-dev cmake make llvm-6.0-dev clang-6.0 build-essential curl

script:
  - ./make-release.sh

notifications:
  email:
    recipients:
      - herberthb12@gmail.com
      - rafa.sa.xp@gmail.com 
    on_success: never # default: change
    on_failure: never # default: always
