# Phase 1.4.3 — Checkpoint TestComp 2026

**Data:** 2026-05-31  
**Branch:** `feat-update`  
**Status:** ✅ Smoke test validado

## Objetivo

Validar o Map2Check v8 (LLVM 16 + New PM + C++17) contra benchmarks reais
do TestComp 2026, garantindo que a migração não introduziu regressões
no pipeline de verificação end-to-end.

## Trabalho Realizado

### Infraestrutura de Teste

1. **Diretório `test-comp2026/`** — Framework completo de checkpoint:
   - `simulation/release/` — Diretório autocontido com binário + libs
   - `simulation/sv-benchmarks/` — Sparse checkout (Loops, Arrays, ControlFlow)
   - `simulation/run_all_inside.sh` — Script de execução dentro do Docker
   - `simulation/release/map2check-wrapper.py` — Wrapper TestComp para v8

2. **Categorias testadas (cover-error-call):**
   - Loops (loops, loop-acceleration, loop-crafted, etc.)
   - Arrays (array-examples, array-industry-pattern, etc.)
   - ControlFlow (eca-rers, ntdrivers, ssh-simplified, etc.)

### Bugs Encontrados e Corrigidos

| # | Bug | Causa Raiz | Fix |
|:--|:----|:-----------|:----|
| 1 | KLEE flags obsoletas | KLEE 3.x removeu `-suppress-external-warnings` e `-use-construct-hash-metasmt` | Atualizar flags no `caller.cpp` |
| 2 | Passes silenciosamente ignorados | New PM pula passes em funções com `optnone` (-O0) sem `isRequired()` | Adicionar `isRequired()=true` em 9 passes |
| 3 | Target function errada | `callPass()` usava env var não lida pelo pass; pass lia de `cl::opt` | Passar `-function-name=` na CLI do opt |

### Detalhamento do Bug 2 (Crítico)

O bug mais importante: no Legacy PM (LLVM 6), o atributo `optnone` era
ignorado para passes registrados como obrigatórios. No New PM (LLVM 16),
o comportamento default mudou — `opt` pula **silenciosamente** qualquer
`FunctionPass` em funções com `optnone`, a menos que o pass declare
`static bool isRequired() { return true; }`.

Como o `clang -O0` marca todas as funções com `optnone`, **nenhum** dos 9
passes de instrumentação era executado. O resultado: o programa nunca era
instrumentado, `main` nunca era renomeado para `__map2check_main__`, e
`llvm-link` falhava com `symbol multiply defined: main`.

### Por que os testes unitários não pegaram?

Os 7 testes unitários testam exclusivamente a **biblioteca backend C**
(BTree, AllocationLog, HeapLog, etc.), não o pipeline de instrumentação
LLVM. Não existiam testes de integração end-to-end que exercitassem o flow
completo: compile → opt (passes) → llvm-link → klee → resultado.

## Resultados do Smoke Test

### Testes individuais validados

| Benchmark | Expected | Result | Correto? |
|:----------|:---------|:-------|:---------|
| `loops/array-1.c` | TRUE | VERIFICATION SUCCEEDED | ✅ |
| `loops/array-2.c` | FALSE | VERIFICATION FAILED | ✅ |

O counter-example gerado para `array-2.c` é correto e inclui valores
simbólicos que levam à violação do assert.

### Smoke test suite (5 por categoria)

> Resultados parciais disponíveis no momento da escrita. Resultado completo
> em `test-comp2026/simulation/resultados_de_testes/summary_report_v8.csv`.

## Arquivos Modificados

### Bugfixes

- `modules/frontend/caller.cpp` — KLEE flags + target function propagation
- `modules/backend/pass/Map2CheckLibrary.hpp` — `isRequired()`
- `modules/backend/pass/NonDetPass.hpp` — `isRequired()`
- `modules/backend/pass/TargetPass.hpp` — `isRequired()`
- `modules/backend/pass/AssertPass.hpp` — `isRequired()`
- `modules/backend/pass/MemoryTrackPass.hpp` — `isRequired()`
- `modules/backend/pass/OverflowPass.hpp` — `isRequired()`
- `modules/backend/pass/TrackBasicBlockPass.hpp` — `isRequired()`
- `modules/backend/pass/LoopPredAssumePass.hpp` — `isRequired()`
- `modules/backend/pass/GenerateAutomataTruePass.hpp` — `isRequired()`

### Infraestrutura TestComp 2026

- `test-comp2026/.gitignore`
- `test-comp2026/README.md` — Manual de reprodução
- `test-comp2026/simulation/release/map2check-wrapper.py`
- `test-comp2026/simulation/run_all_inside.sh`
- `test-comp2026/simulation/run_checkpoint.sh`
- `test-comp2026/simulation/map2check-v8.xml`
- `test-comp2026/simulation/coverage-error-call.prp`
- `test-comp2026/simulation/coverage-branches.prp`

## Próximos Passos

1. Executar suite completo (todas as categorias, sem limite de 5)
2. Gerar relatório comparativo vs TestComp 2023 (v7.3.1)
3. Adicionar testes de integração CI para o pipeline end-to-end
4. Atualizar README principal do projeto
